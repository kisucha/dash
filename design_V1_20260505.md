# Dash 상세 설계 문서

| 필드 | 내용 |
|------|------|
| 문서명 | Dash 상세 설계 문서 |
| 버전 | V1 |
| 날짜 | 2026-05-05 |
| 작성자 | Claude (kisuc 승인) |
| 문서 유형 | 설계 |
| 모델 | claude-sonnet-4-6 |

---

## 1. 전체 흐름

```
[사용자]
  │
  ▼
[Vue 3 SPA — localhost:5173]
  │  Axios REST API 호출
  ▼
[FastAPI 서버 — localhost:8000]
  │  독립 프로세스 spawn
  ├──────────────────────────────────────┐
  ▼                                      ▼
[Pipeline 프로세스]              [APScheduler]
  │  httpx                               │ 크론 트리거
  ▼                                      ▼
[Ollama — localhost:11434]       [Pipeline 프로세스]
  │
  ▼
[SQLite — storage/dash.db]
```

---

## 2. DB 스키마

### tasks 테이블 — 모든 작업 이력

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | INTEGER PK | 자동 증가 |
| feature_id | TEXT | 업무 ID (F001, F002...) |
| status | TEXT | PENDING / RUNNING / DONE / FAILED / CANCELLED |
| params | TEXT | JSON 직렬화된 입력 파라미터 |
| result | TEXT | JSON 직렬화된 결과물 |
| error_message | TEXT | 실패 시 오류 메시지 |
| created_at | DATETIME | 생성 시각 |
| started_at | DATETIME | 실행 시작 시각 |
| finished_at | DATETIME | 완료 시각 |
| triggered_by | TEXT | manual / scheduler |

### schedules 테이블 — 스케줄 설정

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | INTEGER PK | 자동 증가 |
| feature_id | TEXT | 업무 ID |
| cron_expr | TEXT | 크론 표현식 (예: "0 8 * * *") |
| default_params | TEXT | JSON 직렬화된 기본 파라미터 |
| is_active | BOOLEAN | 스케줄 활성 여부 |
| last_run_at | DATETIME | 마지막 실행 시각 |

---

## 3. API 엔드포인트 설계

### Tasks

| 메서드 | 경로 | 설명 |
|--------|------|------|
| POST | /api/tasks | 새 작업 생성 및 실행 |
| GET | /api/tasks | 작업 목록 (페이징) |
| GET | /api/tasks/{id} | 작업 상태 + 결과 조회 |
| DELETE | /api/tasks/{id} | 작업 취소 (RUNNING 상태만) |

### Features

| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | /api/features | 등록된 업무 목록 조회 |
| GET | /api/features/{id} | 업무 상세 (입력 스키마 포함) |

### Schedules

| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | /api/schedules | 스케줄 목록 |
| POST | /api/schedules | 스케줄 등록 |
| PUT | /api/schedules/{id} | 스케줄 수정 |
| DELETE | /api/schedules/{id} | 스케줄 삭제 |

### Health

| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | /api/health | 서버 상태 |
| GET | /api/health/ollama | Ollama 연결 상태 |

---

## 4. 파이프라인 베이스 인터페이스

모든 파이프라인은 아래 인터페이스를 구현해야 합니다.

### 파이프라인 메타데이터 구조

| 필드 | 설명 |
|------|------|
| feature_id | 고유 ID (F001, F002...) |
| name | 표시 이름 |
| description | 업무 설명 |
| input_schema | 입력 파라미터 명세 (필드명, 타입, 필수 여부, 설명) |
| supports_schedule | 스케줄 실행 지원 여부 |

### 파이프라인 실행 계약

- 입력: `task_id` (DB 저장된 ID), `params` (딕셔너리)
- 진행 로그: 단계별 상태를 tasks 테이블 result 컬럼에 갱신
- 완료: status를 DONE으로, result에 최종 결과 저장
- 실패: status를 FAILED로, error_message에 오류 내용 저장
- 중단 감지: tasks 테이블에서 CANCELLED 상태 확인 후 조기 종료

---

## 5. Frontend 화면 구조

### 페이지 목록

| 경로 | 컴포넌트 | 설명 |
|------|----------|------|
| `/` | DashboardView | 업무 카드 목록 + 최근 실행 이력 |
| `/features/:id` | FeatureView | 파라미터 입력 폼 + 실행 버튼 |
| `/tasks/:id` | TaskDetailView | 실행 로그 + 결과 뷰 |
| `/schedules` | ScheduleView | 스케줄 목록 + 등록/수정 |

### 상태 폴링 전략

- TaskDetailView: 2초 간격으로 `/api/tasks/{id}` 폴링
- DashboardView: 10초 간격으로 `/api/tasks` 폴링 (최근 10개)
- DONE/FAILED 상태 도달 시 폴링 중단

---

## 6. Ollama 연동 규칙

| 항목 | 내용 |
|------|------|
| 기본 엔드포인트 | `http://localhost:11434` |
| 헬스체크 | `GET /api/tags` → 200이면 정상 |
| 타임아웃 | 기본 120초 (파이프라인 설정으로 오버라이드 가능) |
| 스트리밍 | `/api/generate` stream=true 사용 |
| 모델 설정 | 파이프라인별 `config.json`에서 지정 |

---

## 7. 첫 번째 파이프라인 선택 — F002 우선

F002(매일 아침 이슈 발굴)를 Phase 2 첫 구현 대상으로 선택.

**이유:**
- 입력 파라미터가 단순 (키워드 목록, 날짜)
- 스케줄러 연동까지 한 번에 검증 가능
- Ollama 연동 패턴을 가장 명확하게 확인할 수 있음

### F002 입력 파라미터

| 파라미터 | 타입 | 필수 | 설명 |
|---------|------|------|------|
| keywords | 문자열 목록 | 아니오 | 관심 키워드 (기본: 주식, AI, 기술) |
| date | 날짜 문자열 | 아니오 | 발굴 기준일 (기본: 오늘) |
| max_issues | 정수 | 아니오 | 최대 이슈 수 (기본: 5) |

### F002 출력

- 이슈 제목, 요약, 출처, 중요도 점수 목록

---

## 8. 개발 순서 (Phase 1 → Phase 2)

### Phase 1 — 뼈대 구축

| 순서 | 작업 | 담당 에이전트 |
|------|------|--------------|
| 1 | FastAPI 기본 앱 + DB 초기화 | api-builder |
| 2 | tasks/features 라우터 + 더미 응답 | api-builder |
| 3 | Vue 3 프로젝트 셋업 + 라우터 | web-builder |
| 4 | DashboardView + FeatureView 기본 UI | web-builder |
| 5 | Ollama 헬스체크 연동 | api-builder |
| 6 | 파이프라인 베이스 클래스 | pipeline-builder |
| 7 | 전체 연동 테스트 | critic |

### Phase 2 — F002 파이프라인

| 순서 | 작업 | 담당 에이전트 |
|------|------|--------------|
| 1 | F002 파이프라인 구현 | pipeline-builder |
| 2 | APScheduler 연동 | api-builder |
| 3 | TaskDetailView 결과 표시 | web-builder |
| 4 | ScheduleView 구현 | web-builder |
| 5 | 종합 검토 | critic |
