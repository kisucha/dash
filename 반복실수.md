# Dash 오류노트 — 반복 실수 방지 체크리스트

| 필드 | 내용 |
|------|------|
| 문서명 | 반복실수.md |
| 버전 | V1 |
| 작성일 | 2026-05-05 |
| 작성자 | Claude |
| 문서 유형 | 오류 방지 가이드 |

> **규칙**: 동일 유형의 버그를 수정하기 전에 반드시 이 파일을 읽고 방지 체크를 수행한다.

---

## [ERR-001] 추측으로 수정 — 실제 원인 미확인

| 항목 | 내용 |
|------|------|
| 실수 유형 | 근거 없이 코드를 수정한 후 "해결됐을 것"이라고 보고 |
| 최초 발생 | 2026-05-05 |
| 근본 원인 | 브라우저 동작을 직접 확인하지 않고 코드 논리로만 판단 |

**방지 절차 (코드 수정 전 반드시 수행):**
1. 백엔드 로그로 API 요청이 실제로 오는지 확인
2. 응답 코드와 바디가 올바른지 curl로 직접 검증
3. Vite dev server에서 프론트엔드 모듈이 최신 코드로 로드됐는지 확인
4. 가설을 세우고 → 가설마다 검증 방법 명시 → 검증 후 수정

---

## [ERR-002] CSS overflow + 고정 높이 레이아웃에서 스크롤 리셋 누락

| 항목 | 내용 |
|------|------|
| 실수 유형 | `overflow-y: auto` + 고정 `height`를 가진 컨테이너에서 라우트 이동 시 scrollTop 미리셋 |
| 최초 발생 | 2026-05-05 |
| 근본 원인 | App.vue 레이아웃을 `window` 스크롤 → 내부 요소 스크롤로 변경하면서 Vue Router의 자동 스크롤 복원이 동작하지 않게 됨 |

**증상:** 페이지 이동 후 콘텐츠가 보이지 않음 (스크롤이 이전 위치에 고정)

**방지 절차:**
- `overflow-y: auto`를 가진 요소에 스크롤이 생기는 레이아웃이면 → 라우트 변경 감지 후 `element.scrollTop = 0` 명시적 리셋 필수
- `router/index.js`의 `scrollBehavior`는 `window` 스크롤만 제어하므로 내부 요소 스크롤에는 무효

---

## [ERR-003] 상태 업데이트 체인 의존성 — 독립 실패 처리 누락

| 항목 | 내용 |
|------|------|
| 실수 유형 | `Promise.all`로 여러 API를 묶으면 하나가 느리면 모두 대기, 하나가 실패하면 catch 전파 |
| 최초 발생 | 2026-05-05 |
| 근본 원인 | `ollamaStatus`가 `checkOllamaHealth()` 결과에만 의존하도록 설계 — 더 빠른 `/api/models`와 연계 안 됨 |

**방지 절차:**
- 상태 업데이트를 단일 소스에만 의존하지 말 것
- 여러 API 중 빠른 API가 성공하면 상태를 'ok'로 먼저 전환 가능한지 검토
- 초기화 코드에서 `Promise.all` 대신 개별 실행 + 각 catch 격리 사용

---

## [ERR-004] 브라우저 focus 동작 오해 — window focus와 element focus 혼동

| 항목 | 내용 |
|------|------|
| 실수 유형 | `onMounted`에서 `element.focus()`를 호출해도 브라우저 창이 포커스를 잃으면 element focus도 해제됨 |
| 최초 발생 | 2026-05-05 |
| 근본 원인 | window focus 이벤트를 감지하지 않고 마운트 시 한 번만 포커스 설정 |

**방지 절차:**
- 창 복귀 시 포커스가 필요하면 `window` + `document` 이벤트 모두 등록
- `window.addEventListener('focus', ...)` — 브라우저 창 복귀
- `document.addEventListener('visibilitychange', ...)` — 탭 복귀

---

## [ERR-005] 서버 hot-reload 미확인 후 테스트

| 항목 | 내용 |
|------|------|
| 실수 유형 | 코드 수정 후 서버가 새 코드를 실제로 로드했는지 확인 없이 테스트 |
| 최초 발생 | 2026-05-05 |
| 근본 원인 | `--reload` 옵션으로 실행 중인 서버가 새 파일(신규 라우터)을 감지 못함 + 구 버전 실행 중 |

**방지 절차:**
1. 새 파일 추가 시 `/openapi.json`에서 엔드포인트 존재 여부 반드시 확인
2. 서버 재시작 후 첫 테스트로 `curl /openapi.json | python -c "print paths"` 실행
3. 응답에 신규 엔드포인트 없으면 재시작 필수

---

## [ERR-006] CSS flex 레이아웃에서 독립 스크롤 미작동 — 빈 화면처럼 보이는 원인

| 항목 | 내용 |
|------|------|
| 실수 유형 | `#app-wrapper`에 `min-height: 100vh`를 쓰면 페이지 전체가 window 레벨로 스크롤됨 |
| 최초 발생 | 2026-05-05 |
| 근본 원인 | `min-height`는 컨테이너가 뷰포트보다 커질 수 있어 window 스크롤을 막지 못함. 페이지 이동 후 window scroll이 이전 위치에 고정되어 콘텐츠가 뷰포트 위로 사라짐 (빈 화면처럼 보임). 패널도 함께 스크롤됨. |

**증상:** 페이지 이동 후 왼쪽 패널이 빈 화면, 두 패널이 같이 스크롤됨

**방지 절차:**
- 뷰포트 고정 레이아웃: `#app-wrapper { height: 100vh; overflow: hidden }` — `min-height` 절대 금지
- flex 자식이 독립 스크롤하려면 부모에 `min-height: 0` 필수 (`height: calc(...)` 명시 불필요)
- 각 패널에 `overflow-y: auto` 있으면 window 스크롤 없이 패널별 독립 스크롤 동작 확인

---

## 수정 전 체크리스트 (매번 확인)

```
[ ] 1. 백엔드 로그에서 API 요청이 오는지 확인했는가?
[ ] 2. curl로 직접 API 응답을 검증했는가?
[ ] 3. 프론트엔드 Vite 모듈이 최신인지 확인했는가?
[ ] 4. CSS 스크롤 컨테이너가 있으면 라우트 변경 시 scrollTop 리셋 확인했는가?
[ ] 5. 상태 업데이트가 단일 소스에만 의존하지 않는지 확인했는가?
[ ] 6. window focus 이벤트를 처리해야 하는 곳에서 처리하고 있는가?
[ ] 7. 서버 재시작이 필요한지 확인했는가? (신규 파일 추가 시)
[ ] 8. RouterView에 :key="route.path"가 있는가? (컴포넌트 재마운트 보장)
```
